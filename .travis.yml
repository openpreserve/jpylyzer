language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"

install:
  - sudo pip install --use-mirrors pyinstaller
  - ./build-apt-package.sh
  - ./dist/jpylyzer --version

script:
  - python -m unittest discover
