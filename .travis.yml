language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"

install:
  - sudo pip install --use-mirrors pyinstaller
  - make clean
  - make
  - python -m unittest discover
  - dpkg-buildpackage -tc

script:
  - python -m unittest discover
