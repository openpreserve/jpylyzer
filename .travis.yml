language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"

install:
  - sudo pip install --use-mirrors pyinstaller
  - pyi-makespec --strip --onefile ./jpylyzer/jpylyzer.py
  - pyinstaller --strip jpylyzer.spec

script:
  - python -m unittest discover
